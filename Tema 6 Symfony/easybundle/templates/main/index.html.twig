{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif;}
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

<!-- crear un form indiependiente de las entidades, con post-->
    <form action="/buscar" method="POST">
        <input type="text" name="txtBuscar"/>
        <input type="submit" name="buscar" value="Buscar"/>
    </form>
<!-------------->

    {% for p in pokemon %}

    <!--Para borrar de forma asincrona-->
    <!--<button onclick="borrar({{p.id}})">X</button>-->

        <a href="/{{p.id}}">{{p.name}} </a>(
        {% for t in p.type %}
            <a href="/type/{{t.id}}">{{t.name}}</a>
        {% endfor %}
        )
        <br>
        {% if p.evolvedFrom != NULL %}
        Evoluciona de {{p.evolvedFrom.name}} {#.name porque el evolved devuelve un objeto pokemon#}
        {% endif %}

    {% endfor %}

</div>

<script>
    function borrar(id){

    }
</script>

{% endblock %}
